services:
        db:
        image: postgres:16
        container_name: postgress_db
        environment:
          POSTGRES_USER: user
          POSTGRES_PASSWORD: password
          POSTGRES_DB: prisma
        ports:
          - "5432:5432"
        volumes:
          - postgres_data:/ C:\Users\kasam\OneDrive\Documents\Backend\todo\backend

volumes:
  postgres_data: